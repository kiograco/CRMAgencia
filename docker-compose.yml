services:
  postgres:
    image: postgres:16-alpine
    container_name: cruise-crm-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: crm_dev
      POSTGRES_USER: crm
      POSTGRES_PASSWORD: crm
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U crm -d crm_dev"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
